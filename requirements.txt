# Core AI-orchestration dependencies
mcp>=1.2.0            # Official Model Context Protocol Python SDK (server + stdio transport)
chromadb>=0.5.0      # Vector database for embeddings storage + semantic search

# Optional: enable LLM-generated answers in the RAG `ask` command.
# The pipeline runs fully offline without these (it returns retrieved context instead).
#   pip install anthropic    &&    export ANTHROPIC_API_KEY=...
# anthropic>=0.39.0
